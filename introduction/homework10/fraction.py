class fraction :
    def __init__ (self , numerator , denominator) :
        self.numerator = numerator
        self.denominator = denominator
    

    def sum (self , fraction1 , fraction2 ):
        #sum 2 fraction
        # sum_numerator = fraction1.numerator 
        # #* selffraction2.denominator + fraction2.numerator * fraction1.denominator
        # print(sum_numerator)
        ...
    
    def minus (self , fraction1 , fraction2 ):
        #subtract 2 fraction
        ...

    def division (self , fraction1 , fraction2 ):
        #divide 2 fraction
        ...
    
    def multiply (self , fraction1 , fraction2 ):
        #multiply 2 fraction
        ...

    def decimal (self , fraction1 ):
        #convert a fraction to decimal number
        ...
    
    def simplify (self , fraction1 ):
        #simplification a fraction
        ...

fraction1 = fraction (int(input ("enter your numerator  ")) , int(input ("enter your denominator")) )
fraction2 = fraction (int(input ("enter your numerator  ")) , int(input ("enter your denominator")) )
